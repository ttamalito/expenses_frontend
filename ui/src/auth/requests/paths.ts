import { BACKEND_URL } from '../../constants';

export const signUpPath = `${BACKEND_URL}/auth/signup`;

export const loginPath = `${BACKEND_URL}/auth/login`;

export const isLoggedInPath = `${BACKEND_URL}/auth/loggedIn`;
