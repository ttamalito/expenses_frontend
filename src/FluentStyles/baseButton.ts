import {makeStyles, shorthands} from "@fluentui/react-components";

const useButtonStyles = makeStyles({
    button: {
        backgroundColor: '#0a788e',
        color: 'white',
    }
});

export default useButtonStyles;